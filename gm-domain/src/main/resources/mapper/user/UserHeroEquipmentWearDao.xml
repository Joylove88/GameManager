<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gm.modules.user.dao.UserHeroEquipmentWearDao">


    <select id="selectPage" resultType="com.gm.modules.user.entity.UserHeroEquipmentWearEntity">
        SELECT
            A.*,
            B2.EQUIP_NAME,
            B2.EQUIP_RARECODE,
            B2.EQUIP_LEVEL,
            C.HERO_NAME,
            E.USER_NAME
        FROM
            GM_USER_HERO_EQUIPMENT_WEAR A
				LEFT JOIN
            gm_user_equipment B ON A.USER_EQUIP_ID = B.USER_EQUIPMENT_ID
        LEFT JOIN
            GM_EQUIPMENT_INFO B2 ON B.EQUIPMENT_ID = B2.EQUIP_ID
        LEFT JOIN
            GM_HERO_INFO C ON A.HERO_ID = C.HERO_ID
        LEFT JOIN
            GM_USER E ON A.USER_ID = E.USER_ID

        ${ew.customSqlSegment}

        ORDER BY E.USER_NAME,C.HERO_NAME,A.CREATE_TIME DESC
    </select>

    <!--获取玩家英雄穿戴中的装备-->
    <select id="getUserWearEQ" resultType="com.gm.modules.user.rsp.UserHeroEquipmentWearRsp">
        SELECT
            A.USER_HERO_EQUIPMENT_WEAR_ID,
            A.HERO_ID,
            A.USER_EQUIP_ID,
            A.USER_HERO_ID,
            B.EQUIPMENT_ID,
            A.PARENT_EQUIP_CHAIN,
            A.`STATUS`,
            B2.EQUIP_NAME,
            B2.EQUIP_RARECODE,
            B2.EQUIP_LEVEL,
            B2.EQUIP_IMG_URL,
            B2.EQUIP_ICON_URL,
            B.EQUIP_POWER,
            B.EQUIP_HEALTH,
            B.EQUIP_HEALTH_REGEN,
            B.EQUIP_MANA,
            B.EQUIP_MANA_REGEN,
            B.EQUIP_ARMOR,
            B.EQUIP_MAGIC_RESIST,
            B.EQUIP_ATTACK_DAMAGE,
            B.EQUIP_ATTACK_SPELL
        FROM
            gm_user_hero_equipment_wear A
        LEFT JOIN
                gm_user_equipment B ON A.USER_EQUIP_ID = B.USER_EQUIPMENT_ID
        LEFT JOIN
                GM_EQUIPMENT_INFO B2 ON B.EQUIPMENT_ID = B2.EQUIP_ID
        <where>
            <if test="status != null and status != ''">
                AND A.`STATUS` = #{status}
            </if>
            <if test="statusB != null and statusB != ''">
                AND B.`STATUS` = #{statusB}
            </if>
            <if test="userHeroId != null and userHeroId != ''">
                AND A.USER_HERO_ID = #{userHeroId}
            </if>
            <if test="parentEquipChain != null and parentEquipChain != ''">
                AND A.PARENT_EQUIP_CHAIN LIKE '%${parentEquipChain}%'
            </if>
        </where>
    </select>


</mapper>