<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gm.modules.basicconfig.dao.HeroEquipmentDao">

    <select id="selectPage" resultType="com.gm.modules.basicconfig.entity.HeroEquipmentEntity">
        SELECT
            A.*,
            B.EQUIP_NAME,
            B.EQUIP_RARECODE,
            B.EQUIP_LEVEL,
            C.HERO_NAME
        FROM
            GM_HERO_EQUIPMENT A
        LEFT JOIN
            GM_EQUIPMENT_INFO B ON A.EQUIP_ID = B.EQUIP_ID
        LEFT JOIN
            GM_HERO_INFO C ON A.HERO_ID = C.HERO_ID

        ${ew.customSqlSegment}
        ORDER BY C.HERO_NAME,A.EQUIP_SOLT,A.CREATE_TIME DESC
    </select>

    <select id="getHeroEquipments" resultType="com.gm.modules.basicconfig.entity.HeroEquipmentEntity">
        SELECT
            A.*,
            B.EQUIP_NAME,
            B.EQUIP_RARECODE,
            B.EQUIP_LEVEL,
            B.EQUIP_IMG_URL,
            B.EQUIP_ICON_URL,
            B.EQUIP_JSON
        FROM
            GM_HERO_EQUIPMENT A
        LEFT JOIN
            GM_EQUIPMENT_INFO B ON A.EQUIP_ID = B.EQUIP_ID
        <where>
            <if test="status != null and status != ''">
                AND A.STATUS = #{status}
            </if>
            <if test="heroId != null and heroId != ''">
                AND A.HERO_ID = #{heroId}
            </if>
        </where>
        ORDER BY A.EQUIP_SOLT
    </select>
</mapper>