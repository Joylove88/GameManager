<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gm.modules.basicconfig.dao.HeroSkillDao">

    <select id="selectPage" resultType="com.gm.modules.basicconfig.entity.HeroSkillEntity">
        SELECT
            A.*,
            B.HERO_NAME
        FROM
            GM_HERO_SKILL A
        LEFT JOIN GM_HERO_INFO B ON A.HERO_ID = B.HERO_ID


        ${ew.customSqlSegment}
        ORDER BY B.HERO_NAME ASC,A.SKILL_STAR_CODE ASC
    </select>

    <!--获取英雄技能信息-->
    <select id="getHeroSkill" resultType="com.gm.modules.basicconfig.entity.HeroSkillEntity">
        SELECT
            A.*
        FROM
            GM_HERO_SKILL A
        <where>
            <if test="status != null and status != ''">
                AND A.STATUS = #{status}
            </if>
            <if test="heroId != null and heroId != ''">
                AND A.HERO_ID = #{heroId}
            </if>
            <if test="skillStarCode != null and skillStarCode != ''">
                AND A.SKILL_STAR_CODE = #{skillStarCode}
            </if>
        </where>
    </select>
</mapper>